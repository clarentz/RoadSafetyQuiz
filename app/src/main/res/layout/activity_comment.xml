<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="msu.olive.Activity.CommentActivity">

    <android.support.v7.widget.Toolbar
        android:layout_width="match_parent"
        android:layout_height="?actionBarSize"
        android:background="@color/colorPrimaryDark"
        >

    </android.support.v7.widget.Toolbar>
    <LinearLayout
        android:background="@color/yellowish"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:weightSum="7">
        <EditText
            android:drawablePadding="5dp"
            android:background="@drawable/x_box_outline"
            android:paddingLeft="10dp"
            android:textSize="14sp"
            android:textColorHint="@color/darkoliveish"
            android:hint="Your comment..."
            android:drawableLeft="@drawable/ic_comment"
            android:id="@+id/comment_content"
            android:padding="5dp"
            android:layout_width="0dp"
            android:layout_height="56dp"
            android:layout_weight="5"/>
        <Button
            android:drawableStart="@drawable/ic_send_comment"
            android:textColor="@color/darkoliveish"
            android:background="@drawable/x_box_outline"
            android:id="@+id/comment_button_send"
            android:text="Send"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="2"/>
    </LinearLayout>
    <LinearLayout
        android:orientation="vertical"
        android:weightSum="10"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <ListView

            android:dividerHeight="2dp"
            android:id="@+id/comment_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"></ListView>

    </LinearLayout>

</LinearLayout>
